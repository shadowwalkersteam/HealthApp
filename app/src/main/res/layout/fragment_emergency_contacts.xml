<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="?attr/actionBarSize"
    tools:context="com.example.zohai.Fragments.EmergencyContacts">

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:clickable="true"
        app:fabSize="normal"
        app:srcCompat="@drawable/ic_add_white_24dp"
        android:layout_marginEnd="17dp"
        android:layout_marginBottom="26dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true" />

    <include layout="@layout/contact_item"
        android:id="@+id/include" />

        <Button
            android:id="@+id/updatenumbers"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:background="@drawable/rect3"
            android:layout_below="@+id/include"
            android:gravity="center_horizontal"
            android:layout_centerHorizontal="true"
            android:padding="16dp"
            android:clickable="true"
            android:textColor="#fff"
            android:text="Update Contacts"
            android:textSize="16dp"
            android:visibility="invisible"
            />
    <Button
        android:id="@+id/delnumbers"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="@drawable/rect3"
        android:layout_below="@+id/updatenumbers"
        android:gravity="center_horizontal"
        android:layout_centerHorizontal="true"
        android:padding="16dp"
        android:clickable="true"
        android:textColor="#fff"
        android:text="Delete Contacts"
        android:textSize="16dp"
        android:visibility="visible"
        />

</RelativeLayout>
